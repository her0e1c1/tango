import { test } from "./utils/fixtures";

test.fixme("CARD-FILTER-01 学習条件にかかわらず Card を全件表示できる", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-02 他の Deck のフィルター条件にかかわらず Card を全件表示できる", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-03 Card が登録されていない Deck は0件表示になる", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-04 フィルターに一致する Card がない場合は0件表示になる", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-05 リロード後も同じタグと AND / OR 条件を復元できる", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-06 フィルターを解除した状態もリロード後に維持できる", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-07 単一タグで Card を絞り込める", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-08 AND 条件ですべての選択タグを持つ Card に絞り込める", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-09 OR 条件でいずれかの選択タグを持つ Card に絞り込める", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-10 絞り込み結果を追加が新しい順にソートできる", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});

test.fixme("CARD-FILTER-11 絞り込み結果の表示順を標準へ戻せる", () => {
  // Pending behavior specification; no implementation is added by this file-only migration.
});
