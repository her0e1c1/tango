/** @public Testing-only entry point for installing deterministic remote Deck snapshots. */
export { replaceRemoteDecks } from "./model/store";
export type { LocalDeck, RemoteDeck } from "./model/types";
