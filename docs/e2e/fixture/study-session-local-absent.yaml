extends: local-deck-with-cards.yaml
browser:
  absentCards:
    - id: missing-card
      deckId: deck-1
  studySessions:
    deck-1:
      sessionId: session-1
      deckId: deck-1
      cardOrderIds: [missing-card]
      currentIndex: 0
