# Study Session Store 単体テスト仕様書

## 目的

デックごとの学習の続きを参照でき、削除・所有者変更・取得完了によって他のデックや利用者のセッションを混同しないことを確認する。保存した変更を取得するまでの再開対象、および学習の取得中・取得成功・取得失敗で変わる待機状態も確認する。カードの選定規則、FSRS、Firestore への実際の保存、認証に伴う購読の開始・停止は対象外とする。

関連 E2E: [STUDY-SESSION-01](../../e2e/study-session.md#study-session-01)、[STUDY-ACTIONS-04](../../e2e/study-actions.md#study-actions-04)、[ACCOUNT-04](../../e2e/account.md#account-04)

共通の検証前提は [AGENTS.md](./AGENTS.md) を参照する。

取得済みデータと同期位置の保存・復元は [差分同期](../../integration/firestore/incremental-sync.md) と [Persistence](../../e2e/persistence.md) で確認する。

## テストケース

| ID | カテゴリ | 区分 | テストケース |
| --- | --- | --- | --- |
| UNIT-STORE-STUDY-01 | state-change | 正常系 | [デックごとの学習の続きが互いに混ざらない](#unit-store-study-01) |
| UNIT-STORE-STUDY-02 | state-change | 正常系 | [最新の取得結果にないデックを学習の続きから除外する](#unit-store-study-02) |
| UNIT-STORE-STUDY-03 | scope-reset | 正常系 | [学習中の情報をクリアしても旧バックアップを失わない](#unit-store-study-03) |
| UNIT-STORE-STUDY-04 | scope-reset | 正常系 | [所有者を変更したら一致しない学習の続きを提供しない](#unit-store-study-04) |
| UNIT-STORE-STUDY-05 | state-change | 正常系 | [最新の取得結果に学習の続きを切り替えて取得待ちを終了する](#unit-store-study-05) |
| UNIT-STORE-STUDY-06 | state-change | 正常系 | [取得待ちだけを終了すると保持済みの学習の続きは変わらない](#unit-store-study-06) |
| UNIT-STORE-STUDY-07 | state-change | 正常系 | [初めて始めた学習は購読通知を受けて再開対象に加わる](#unit-store-study-07) |
| UNIT-STORE-STUDY-08 | state-change | 正常系 | [次へ進んだ学習は購読通知を受けて次の位置になる](#unit-store-study-08) |
| UNIT-STORE-STUDY-09 | state-change | 正常系 | [やり直した学習は購読通知を受けて新しい学習に切り替わる](#unit-store-study-09) |
| UNIT-STORE-STUDY-10 | state-change | 正常系 | [購読開始から初回の結果通知まで取得待ちになる](#unit-store-study-10) |
| UNIT-STORE-STUDY-11 | state-change | 異常系 | [購読エラーで取得待ちを終えて取得済みの続きを維持する](#unit-store-study-11) |
| UNIT-STORE-STUDY-12 | state-change | 正常系 | [指定した学習位置は購読通知を受けて参照できる](#unit-store-study-12) |
| UNIT-STORE-STUDY-13 | state-change | 正常系 | [学習を再開した時刻は購読通知を受けて更新される](#unit-store-study-13) |
| UNIT-STORE-STUDY-14 | state-change | 正常系 | [完了した学習は購読通知を受けて再開対象から外れる](#unit-store-study-14) |
| UNIT-STORE-STUDY-15 | state-change | 正常系 | [中断した学習は購読通知を受けて再開対象から外れる](#unit-store-study-15) |

<a id="unit-store-study-01"></a>

### UNIT-STORE-STUDY-01 [TODO] デックごとの学習の続きが互いに混ざらない

カテゴリ: `state-change`

区分: 正常系

Given:

同じ利用者に、デック A のセッション（カード順 a1、a2、学習の続きは 2 枚目）と、デック B のセッション（カード順 b1、学習の続きは 1 枚目）がある。両者のセッション識別子と最終学習時刻も異なる。

When:

本人の学習の購読を開始した後、外部データソースから両セッションを含む取得結果が通知される。

Then:

公開 query でデック A と B を指定した参照結果は、それぞれのセッション識別子、カード順、学習位置、最終学習時刻に一致する。互いの値を上書きせず、存在しないデック C のセッションは取得できない。

<a id="unit-store-study-02"></a>

### UNIT-STORE-STUDY-02 [TODO] 最新の取得結果にないデックを学習の続きから除外する

カテゴリ: `state-change`

区分: 正常系

Given:

デック A と B に、カード順・学習位置・最終学習時刻が異なるセッションを保持している。

When:

本人の学習の購読を開始し、外部データソースから A と B を取得済みの状態で、B のセッションだけを含む最新の取得結果が通知される。

Then:

公開 query では A だけが取得できなくなり、B の識別子・カード順・学習位置・最終学習時刻は変更されない。

<a id="unit-store-study-03"></a>

### UNIT-STORE-STUDY-03 [TODO] 学習中の情報をクリアしても旧バックアップを失わない

カテゴリ: `scope-reset`

区分: 正常系

Given:

複数デックのセッションを保持して取得待ちであり、以前の保存形式の学習バックアップがある。既にセッションがなく取得待ちでもない場合も独立して用意する。

When:

学習セッションモデルのクリア操作を行う。

Then:

どのデックのセッションも取得できず、取得待ちではなくなる。既に空でも操作は失敗しない。旧バックアップの内容は変更・削除されない。

<a id="unit-store-study-04"></a>

### UNIT-STORE-STUDY-04 [TODO] 所有者を変更したら一致しない学習の続きを提供しない

カテゴリ: `scope-reset`

区分: 正常系

Given:

利用者 a のデック A と B のセッションを保持している。各行を独立して検証する。

| 新しい所有者 | 変更後に取得できるセッション | 取得待ち |
| --- | --- | --- |
| 利用者 a | A、B を変更せず維持 | はい |
| 利用者 b | なし | はい |
| 所有者なし | なし | いいえ |

When:

学習セッションモデルへ新しい所有者を通知する。

Then:

参照結果と取得待ちの有無は表に一致する。同じ所有者の再取得では学習位置などを失わず、別の所有者や所有者なしに変わった場合は以前の利用者の学習の続きを提供しない。

<a id="unit-store-study-05"></a>

### UNIT-STORE-STUDY-05 [TODO] 最新の取得結果に学習の続きを切り替えて取得待ちを終了する

カテゴリ: `state-change`

区分: 正常系

Given:

同じ所有者のデック A、B のセッションを保持して取得待ちである。新しい取得結果として、次の各条件を独立して用意する。

| 新しい取得結果 | 取得できるセッション |
| --- | --- |
| A の更新された学習位置・最終学習時刻と C のセッション | 更新後の A と C のみ |
| セッションなし | なし |

When:

同じ所有者の学習の購読を開始した後、外部データソースから表の新しい取得結果が通知される。

Then:

公開 query の参照結果は表に一致し、以前だけ存在したセッションや A の古い学習位置・最終学習時刻は残らない。結果が空の場合も取得待ちは終了する。

<a id="unit-store-study-06"></a>

### UNIT-STORE-STUDY-06 [TODO] 取得待ちだけを終了すると保持済みの学習の続きは変わらない

カテゴリ: `state-change`

区分: 正常系

Given:

デック A、B のセッションを保持して取得待ちである。セッションを保持せず取得待ちである場合も独立して用意する。

When:

取得結果の差し替えを伴わない取得終了を学習セッションモデルへ通知する。

Then:

取得待ちは終了し、保持済みのセッション識別子、カード順、学習位置、最終学習時刻は変更されない。セッションがなければ、そのまま何も取得できない。取得結果を空として反映する操作とは区別する。

<a id="unit-store-study-07"></a>

### UNIT-STORE-STUDY-07 [TODO] 初めて始めた学習は購読通知を受けて再開対象に加わる

カテゴリ: `state-change`

区分: 正常系

Given:

本人の学習の購読を開始し、外部データソースからの通知でデック A の再開対象がないことを取得済みである。別デック B には識別子・カード順・学習位置・最終学習時刻が異なる学習の続きを取得済みである。

When:

A の学習をカード順 a3、a1 で開始する。変更後の取得結果がまだ通知されていない間の再開対象を参照し、その後、外部データソースから新しい A と既存の B の学習を含む取得結果が通知される。

Then:

通知前の公開 query では A の再開対象は存在しない。通知後は新しい識別子・カード順 a3、a1・1 枚目の位置となり、最終学習時刻は通知された新しい学習の値に一致する。すべての時点で、公開 query から取得できる B の識別子・カード順・学習位置・最終学習時刻は変わらない。

<a id="unit-store-study-08"></a>

### UNIT-STORE-STUDY-08 [TODO] 次へ進んだ学習は購読通知を受けて次の位置になる

カテゴリ: `state-change`

区分: 正常系

Given:

本人の学習の購読を開始し、外部データソースからの通知でA のカード順 a1、a2、a3 の 1 枚目を取得済みである。別デック B には識別子・カード順・学習位置・最終学習時刻が異なる学習の続きを取得済みである。

When:

A を次のカードへ進める。変更後の取得結果がまだ通知されていない間の再開対象を参照し、その後、外部データソースから2 枚目まで進んだ Aと既存の B を含む取得結果が通知される。

Then:

通知前の公開 query では A の識別子・カード順・学習位置・最終学習時刻が操作前のままである。通知後はA は同じ識別子とカード順の 2 枚目となり、最終学習時刻は通知された値になる。すべての時点で、公開 query から取得できる B の識別子・カード順・学習位置・最終学習時刻は変わらない。

<a id="unit-store-study-09"></a>

### UNIT-STORE-STUDY-09 [TODO] やり直した学習は購読通知を受けて新しい学習に切り替わる

カテゴリ: `state-change`

区分: 正常系

Given:

本人の学習の購読を開始し、外部データソースからの通知でA のカード順 a1、a2、a3 の 2 枚目を取得済みである。別デック B には識別子・カード順・学習位置・最終学習時刻が異なる学習の続きを取得済みである。

When:

A をカード順 a3、a1 で最初からやり直す。変更後の取得結果がまだ通知されていない間の再開対象を参照し、その後、外部データソースから以前の学習を中断し新しく始めた Aと既存の B を含む取得結果が通知される。

Then:

通知前の公開 query では A の識別子・カード順・学習位置・最終学習時刻が操作前のままである。通知後はA は以前と異なる識別子・カード順 a3、a1・1 枚目の位置となり、最終学習時刻は通知された新しい学習の値に一致する。すべての時点で、公開 query から取得できる B の識別子・カード順・学習位置・最終学習時刻は変わらない。

<a id="unit-store-study-10"></a>

### UNIT-STORE-STUDY-10 [TODO] 購読開始から初回の結果通知まで取得待ちになる

カテゴリ: `state-change`

区分: 正常系

Given:

取得待ちではなく、利用者 a の A と B の学習を取得済みである。再び取得する所有者は a または b とし、その所有者に学習の続きがある場合とない場合を独立して用意する。

When:

指定した所有者の学習の購読を開始する。初回の取得結果がまだ通知されていない間の状態を参照し、その後、外部データソースからその所有者の取得結果が通知される。

Then:

通知前は公開 query で取得待ちを参照できる。所有者が a なら A と B の内容を維持し、b なら a の学習は参照できない。通知後はその所有者の最新の学習の続きだけを参照でき、結果が空の場合も取得待ちが終了する。

<a id="unit-store-study-11"></a>

### UNIT-STORE-STUDY-11 [TODO] 購読エラーで取得待ちを終えて取得済みの続きを維持する

カテゴリ: `state-change`

区分: 異常系

Given:

本人の学習の購読を開始して初回結果を待っている。同じ所有者の A と B の学習を以前の購読で取得済みの場合と、取得済みの学習がない場合を独立して用意する。A と B は識別子・カード順・学習位置・最終学習時刻が異なる。

When:

初回の取得結果が通知される前に、外部データソースから購読エラーが通知される。

Then:

公開 query では取得待ちが終了し、利用側は購読の失敗を把握できる。取得済みの A と B の識別子・カード順・学習位置・最終学習時刻は変わらず、取得済みの学習がなければ再開対象なしのままである。正常な取得完了としては通知されない。

<a id="unit-store-study-12"></a>

### UNIT-STORE-STUDY-12 [TODO] 指定した学習位置は購読通知を受けて参照できる

カテゴリ: `state-change`

区分: 正常系

Given:

本人の学習の購読を開始し、外部データソースからの通知でA のカード順 a1、a2、a3 の 1 枚目を取得済みである。別デック B には識別子・カード順・学習位置・最終学習時刻が異なる学習の続きを取得済みである。

When:

A の学習位置を 3 枚目に指定する。変更後の取得結果がまだ通知されていない間の再開対象を参照し、その後、外部データソースから3 枚目へ移動した Aと既存の B を含む取得結果が通知される。

Then:

通知前の公開 query では A の識別子・カード順・学習位置・最終学習時刻が操作前のままである。通知後はA は同じ識別子とカード順の 3 枚目となり、最終学習時刻は通知された値になる。すべての時点で、公開 query から取得できる B の識別子・カード順・学習位置・最終学習時刻は変わらない。

<a id="unit-store-study-13"></a>

### UNIT-STORE-STUDY-13 [TODO] 学習を再開した時刻は購読通知を受けて更新される

カテゴリ: `state-change`

区分: 正常系

Given:

本人の学習の購読を開始し、外部データソースからの通知でA のカード順 a1、a2、a3 の 2 枚目を取得済みである。別デック B には識別子・カード順・学習位置・最終学習時刻が異なる学習の続きを取得済みである。

When:

A のカードを進めず、以前の最終学習時刻より後の時刻に再開する。変更後の取得結果がまだ通知されていない間の再開対象を参照し、その後、外部データソースから再開時刻を更新した Aと既存の B を含む取得結果が通知される。

Then:

通知前の公開 query では A の識別子・カード順・学習位置・最終学習時刻が操作前のままである。通知後はA の識別子・カード順・位置は変わらず、最終学習時刻だけが通知された再開時刻になる。すべての時点で、公開 query から取得できる B の識別子・カード順・学習位置・最終学習時刻は変わらない。

<a id="unit-store-study-14"></a>

### UNIT-STORE-STUDY-14 [TODO] 完了した学習は購読通知を受けて再開対象から外れる

カテゴリ: `state-change`

区分: 正常系

Given:

本人の学習の購読を開始し、外部データソースからの通知でA のカード順 a1、a2、a3 の最後のカードを取得済みである。別デック B には識別子・カード順・学習位置・最終学習時刻が異なる学習の続きを取得済みである。

When:

A の最後のカードを完了する。変更後の取得結果がまだ通知されていない間の再開対象を参照し、その後、外部データソースから完了済みの Aと既存の B を含む取得結果が通知される。

Then:

通知前の公開 query では A の識別子・カード順・学習位置・最終学習時刻が操作前のままである。通知後はA の再開対象は存在しなくなる。すべての時点で、公開 query から取得できる B の識別子・カード順・学習位置・最終学習時刻は変わらない。

<a id="unit-store-study-15"></a>

### UNIT-STORE-STUDY-15 [TODO] 中断した学習は購読通知を受けて再開対象から外れる

カテゴリ: `state-change`

区分: 正常系

Given:

本人の学習の購読を開始し、外部データソースからの通知でA のカード順 a1、a2、a3 の 2 枚目を取得済みである。別デック B には識別子・カード順・学習位置・最終学習時刻が異なる学習の続きを取得済みである。

When:

A の学習を明示的に中断する。変更後の取得結果がまだ通知されていない間の再開対象を参照し、その後、外部データソースから中断済みの Aと既存の B を含む取得結果が通知される。

Then:

通知前の公開 query では A の識別子・カード順・学習位置・最終学習時刻が操作前のままである。通知後はA の再開対象は存在しなくなる。すべての時点で、公開 query から取得できる B の識別子・カード順・学習位置・最終学習時刻は変わらない。
