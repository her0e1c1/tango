外部の構造化データは取り込み境界で Zod を標準に検証し、型アサーションだけで信頼しない。形式専用 parser・存在確認も要件に応じて使う。
Entity は不変条件、永続化境界は保存スキーマ、インポート境界は構文・エラーを所有し、型を推論して検証を重複させない。
snapshot は検証後に Store へ反映する。旧形式は明示した互換要件がある場合だけ正規化し、互換性のない永続 Store 状態は破棄する。
