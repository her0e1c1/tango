Form は React Hook Form、表示状態は画面寿命で管理し、画面をまたぐ操作のロック・実行 ID・再試行 ID は非永続の操作状態に残す。
操作が保存・ロック解除・全体通知を担い、再試行は保持した入力から明示的に行う。遷移・フォーカス・表示更新は同じ画面寿命の場合だけ行う。
Toast の寿命は画面から独立させるが、現行 Deck 削除の離脱後通知抑止は例外とする。操作ロックを端末間の整合性保証に使わない。
