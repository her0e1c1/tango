ローカル専用データは UID を持たずブラウザーに、リモートは認証利用者の所有として Firestore に保存し、Deck のモードで子 Card の保存先も決める。
所有者は Form 外で導出・検証し、インポート再試行時も UID 変更を拒否する。Deck・Card の編集・削除は所有者不一致を拒否する。
移行は同じ ID を保つ明示的なローカル→リモート操作だけとし、親子の保存成功までローカルを残す。Firestore キャッシュはローカル専用データと区別する。
